# =========================================================
# 🧩 Professional Backend .gitignore (Node.js + Express + MongoDB + Mongoose + pnpm)
# =========================================================
# 📘 Description:
# This .gitignore file is specifically tailored for backend-only projects
# built with Node.js, Express.js, MongoDB, and Mongoose, using pnpm 
# as the package manager.
#
# ⚙️ Tech Stack Coverage:
# - Node.js (JavaScript)
# - Express.js (Web Framework)
# - MongoDB (Database)
# - Mongoose (ODM)
# - pnpm (Package Manager)
#
# 🎯 Purpose:
# - Prevent sensitive information (e.g., .env files) from being committed.
# - Ignore unnecessary system files, caches, dependencies, and logs.
# - Keep repositories clean and lightweight by excluding build and runtime files.
#
# 🏗 Recommended Usage:
# Place this `.gitignore` file at the root of your backend project.
# It is intended for **JavaScript-based backends only** (no TypeScript or frontend).
#
# =========================================================


# ---------------------------------------------------------
# 🌍 Environment & Configuration Files
# ---------------------------------------------------------
# These files contain secrets or local configs — never push them to Git.
.env
*.env
.env.*
.env.local
.env.development.local
.env.production.local
.env.test.local


# ---------------------------------------------------------
# ⚙️ Node.js / Express / Mongoose / MongoDB
# ---------------------------------------------------------
# Core backend setup — dependencies, debug logs, and runtime data.
node_modules/
bower_components/
jspm_packages/
web_modules/

# Package manager logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Process / PID files
pids/
*.pid
*.seed
*.pid.lock


# ---------------------------------------------------------
# 🧠 MongoDB Data & Dumps
# ---------------------------------------------------------
# Database dumps or local test data should not be versioned.
dump/
*.bson
*.lock
data/
db/


# ---------------------------------------------------------
# 🧰 Build / Output / Temporary Files
# ---------------------------------------------------------
# These are generated automatically — not needed in version control.
dist/
build/
.out/
.output/
.cache/
.temp/
.tmp/
.esbuild/
webpack-cache/
parcel-cache/
lib-cov/
coverage/
.nyc_output/


# ---------------------------------------------------------
# 🪣 pnpm Specific Files
# ---------------------------------------------------------
# pnpm workspace and internal cache directories.
.pnpm/
.pnpm-store/
pnpm-lock.yaml
pnpm-workspace.yaml
.pnpmfile.cjs
.pnpm-cache/
.pnpm-debug.log


# ---------------------------------------------------------
# 🧹 Logs, Temp & Miscellaneous
# ---------------------------------------------------------
# Prevent clutter from temporary or debug files.
*.log
*.tmp
*.bak
*~
*.swp
tmp/
temp/

# System-specific junk files
.DS_Store
Thumbs.db


# ---------------------------------------------------------
# 🧩 IDE / Editor Specific Files
# ---------------------------------------------------------
# Ignore IDE configs except essential VSCode settings.
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.iml

# JetBrains IDEs
.idea/
*.iml

# VSCode (keep workspace settings if needed)
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json


# ---------------------------------------------------------
# 🧪 Testing & Reports
# ---------------------------------------------------------
# Ignore test coverage and generated reports.
coverage/
test-output/
*.lcov


# ---------------------------------------------------------
# 🗂 Archives & Packages
# ---------------------------------------------------------
# Ignore compressed or bundled output files.
*.tgz
*.zip
*.rar
*.tar
*.gz


# ---------------------------------------------------------
# ✅ End of File
# ---------------------------------------------------------
# Clean, secure, and professional .gitignore for backend projects.
# Designed and optimized for:
# Node.js + Express + MongoDB + Mongoose + pnpm
# =========================================================
